/**
 * Use strict: defines that the Javascript code should be executed in 'strict mode'
 * 
 * To use this file: var someVar = require("./../config/environment.js");
 */

'use strict';

module.exports = function(environment) {
  let ENV = {
  };

  return ENV;
};
